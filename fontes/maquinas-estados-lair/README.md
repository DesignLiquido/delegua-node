# Máquinas de Estados

Usadas para interações continuadas entre as partes das linguagens e a interface com o usuário, normalmente implementam o fluxo de execução em modo LAIR, que exige o armazenamento de estados. Por exemplo, quando o usuário informa múltiplas linhas de instrução, as máquinas de estados precisam acumular todo o código antes de executar.